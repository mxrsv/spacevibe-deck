# Docs are hand-maintained prose — prettier must not reformat them.
*.md
# Vendored third-party bundle — never reformatted.
electron/vendor/**
# Separate prototype app — out of scope for the app baseline.
marketing/**
# Binary/image assets — no parser.
src/assets/**
# Test fixture data — bytes are part of the test contract, never formatted.
electron/usage/fixtures/**
# Generated snapshot — rewritten wholesale by refresh:pricing in its own format.
src/lib/usage-pricing-snapshot.ts
# Shell fixtures — no parser.
*.zsh
*.jsonl
# Tauri build artifacts and generated schemas — regenerated by the tauri toolchain.
src-tauri/target/**
src-tauri/gen/**
