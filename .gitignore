# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-gate-m
dist-gate-m-renderer
dist-electron-app
dist-electron-release
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local design docs / specs (không track) — pipeline frozen docs ARE tracked
docs/superpowers/
docs/archive/
# Keep pipeline artifacts:
# docs/PRINCIPLES.md, docs/PRD.md, docs/BUSINESS-FLOW.md, docs/CONTEXT.md,
# docs/CONTEXT-archive.md, docs/ARCHITECTURE.md, docs/UX-DESIGN.md,
# docs/REQUIREMENTS.md, docs/adr/, docs/decisions/

# Local tool state
.playwright-mcp/
.planning/

# Marketing film: intermediate frames are disposable, renders are published
# by hand once a cut is approved.
marketing/video/.frames/
marketing/video/out/

# Local agent configuration and worktree bookkeeping, per-machine.
.claude/

# Electron host build output + local run logs
dist-electron/
*.log

# Design screenshots dropped at the repo root during a session. Review evidence
# belongs in docs/review/assets/ and IS tracked; these are working captures.
/deck-navigation-picker.png
/deck-primary-contrast-8-proposal.png
.vercel
.env*
