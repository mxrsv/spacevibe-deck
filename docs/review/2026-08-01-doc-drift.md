# Doc drift audit — 2026-08-01

Point-in-time record (frozen once written). Audited at HEAD `7df2950` on
2026-08-01, scan requested after the landing download-link work. Scope: living
docs (`AGENTS.md`, `README.md`, `docs/ARCHITECTURE.md`, `docs/CONTEXT.md`)
checked against code and the GitHub releases record. `decided`/`building`
claims were skipped per the docs convention — they are backlog, not drift.

## Layer 1 — anchor check: 5 flags, all tool false positives

`docs-anchors.sh` reported five dead anchors. Each target heading exists; the
script greps the raw fragment string and cannot match GitHub heading slugs
(dots dropped from `10.` / `7.7`, em dash collapsing to `--`).

| Flagged anchor                                                             | Target heading                                                     | Verdict         |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------------- |
| `#10-verification-and-acceptance` (README:31, ARCHITECTURE:10, CONTEXT:87) | `## 10. Verification and acceptance` — spec line 378               | valid on GitHub |
| `#77-window-chrome-and-demo-surface` (CONTEXT:90)                          | `### 7.7 Window chrome and demo surface` — spec line 309           | valid on GitHub |
| `#windows-engineering-preview--2026-07-29` (ARCHITECTURE:90)               | `## Windows engineering preview — 2026-07-29` — CONTEXT.md line 46 | valid on GitHub |

Follow-up (outside this repo): teach `~/.claude/scripts/docs-anchors.sh` to
slugify headings before matching, or the same three anchors will flag on every
run.

## Layer 2 — `current` claims vs reality

### Drift found

| #   | Claim                                                                                       | Source                           | Intent    | Status         | Evidence                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------- | -------------------------------- | --------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | "No Windows installer has been staged … no manual dispatch was performed"                   | `docs/CONTEXT.md` Delivery state | `current` | `contradicted` | Prerelease [v0.9.0-windows-preview](https://github.com/mxrsv/spacevibe-deck/releases/tag/v0.9.0-windows-preview) published 2026-07-31T17:12Z with `SpaceVibe.Deck_0.9.0_x64-setup.exe`; an end user installed it on 2026-08-01 |
| 2   | "Signing and any public Windows distribution require a separate decision and authorization" | `docs/CONTEXT.md` Delivery state | `current` | `contradicted` | The prerelease above is public, and the landing links it directly since `4689e83` / `7df2950`                                                                                                                                  |
| 3   | "installer, runtime, screenshot, signing, and publication gates remain pending"             | `README.md` Windows status       | `current` | `partial`      | Installer and publication happened de facto (unsigned, preview-labeled prerelease); runtime QA, screenshots and signing remain pending                                                                                         |
| 4   | "v0.8.0 release is waiting on a pushed `v0.8.0` tag"                                        | `AGENTS.md` In flight            | `current` | `contradicted` | No `v0.8.0` tag was ever released; releases jumped v0.7.0 → [v0.9.0](https://github.com/mxrsv/spacevibe-deck/releases/tag/v0.9.0) (Latest, 2026-07-27)                                                                         |

Unknown, not guessed: `ci.yml:118-124` still gates installer builds on a
private repository while `gh repo view` reports PUBLIC — how the published
`.exe` was produced is not verifiable from reachable history alone.

### Checked and matching (no drift)

- `docs/ARCHITECTURE.md` module table paths; `info.rs#L10-L34`
  (`PaneProcessKind`); `shell.rs#L84-L113` (PowerShell 7 preference);
  `WINDOWS_KEYMAP` at `action-registry.ts:678` within the cited L670–L736;
  `commands` table at `tab-manager.ts:960` within the cited L946–L1024.
- `docs/CONTEXT.md` existing ledger row: exactly 4 `FR-`/`ADR` comments remain
  (`agents.rs`, `open-board.tsx`, `migrate.rs`).
- `generate:menu:check` present in `ci.yml` (lines 34, 86); privacy guard
  present (`ci.yml:118-124`); `AGENTS.md` npm commands all exist in
  `package.json`.

## Decisions pending (ranked by risk if left as-is)

1. Rewrite the `docs/CONTEXT.md` Delivery state and `README.md` Windows status
   to match the published preview (body-text edits — separate approval).
2. Replace the stale v0.8.0 line in `AGENTS.md` In flight.
3. Fix the `docs-anchors.sh` slugifier (lives in `~/.claude`, outside this
   repo).
